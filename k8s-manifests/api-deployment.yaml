apiVersion: v1
kind: Namespace
metadata:
  name: mlops-production
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlops-fraud-api
  namespace: mlops-production
  labels:
    app: fraud-api
    tier: inference
spec:
  replicas: 2 # Alta disponibilidad: 2 pods corriendo en paralelo
  selector:
    matchLabels:
      app: fraud-api
  template:
    metadata:
      labels:
        app: fraud-api
    spec:
      containers:
      - name: api-engine
        # REEMPLAZA 'tu-usuario-docker' por tu usuario real de Docker Hub
        image: tu-usuario-docker/mlops-fraud-api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        # Recursos asignados para evitar que tiren el nodo de KinD (Pure Ops Estricto)
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "200m"
            memory: "256Mi"
        # Probes nativos acoplados a los endpoints que programaste en FastAPI
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: mlops-fraud-api-svc
  namespace: mlops-production
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: fraud-api
